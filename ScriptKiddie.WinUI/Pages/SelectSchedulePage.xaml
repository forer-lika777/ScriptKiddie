<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScriptKiddie.WinUI.Pages.SelectSchedulePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScriptKiddie.WinUI.Pages"
    xmlns:models="using:ScriptKiddie.WinUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <ListView x:Name="ScheduleItemList" Margin="20, 40" SelectionChanged="ScheduleItemList_SelectionChanged"
                  ItemsSource="{x:Bind ViewModel.SelectSchedules, Mode=OneWay}" SelectionMode="Extended">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:SelectSchedule">
                    <StackPanel Padding="15" Spacing="5">
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontSize="20"/>
                        <StackPanel Orientation="Horizontal" Spacing="5">
                            <TextBlock Text="{x:Bind ScheduleTime.StartTime.ToString(), Mode=OneWay}" Foreground="LightGray"/>
                            <TextBlock Text="~" Foreground="LightGray"/>
                            <TextBlock Text="{x:Bind ScheduleTime.EndTime.ToString(), Mode=OneWay}" Foreground="LightGray"/>
                        </StackPanel>
                        <TextBlock Text="{x:Bind local:SelectSchedulePage.SelectTypeToString(SelectType), Mode=OneWay}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="5" Visibility="{x:Bind ViewModel.HasCourseSelectSchedule, Converter={StaticResource BoolToReversedVisibilityConverter}, Mode=OneWay}">
            <TextBlock Text="当前还没有选课时间表" HorizontalAlignment="Center"/>
            <TextBlock Text="点击下方按钮来创建一个选课时间表" HorizontalAlignment="Center" Foreground="LightGray"/>
        </StackPanel>
        <CommandBar IsOpen="False" DefaultLabelPosition="Right" VerticalAlignment="Bottom" Margin="30">
            <AppBarButton x:Name="AddButton" Icon="Add" Label="添加" Click="AddButton_Click"/>
            <AppBarButton x:Name="EditButton" Icon="Edit" Label="编辑" Click="EditButton_Click"/>
            <AppBarButton x:Name="DeleteButton" Icon="Delete" Label="删除" Click="DeleteButton_Click"/>
        </CommandBar>
    </Grid>
</Page>
