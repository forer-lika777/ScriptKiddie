<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ScriptKiddie.WinUI.Pages.Controls.SelectScheduleSelectPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScriptKiddie.WinUI.Pages.Controls"
    xmlns:models="using:ScriptKiddie.WinUI.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView x:Name="SelectSchedulesListView" ItemsSource="{x:Bind ViewModel.SelectSchedules}" 
                  SelectedItem="{x:Bind ViewModel.SelectedSchedule, Mode=TwoWay}" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:SelectSchedule">
                    <StackPanel Spacing="5" Margin="5, 12">
                        <TextBlock Text="{x:Bind Name}"/>
                        <TextBlock Text="{x:Bind ScheduleTime.ToString()}"/>
                        <TextBlock Text="{x:Bind SelectType, Converter={StaticResource SelectTypeToStringConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
