<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ScriptKiddie.WinUI.Pages.AccountManagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScriptKiddie.WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="40, 80" Spacing="20">
        <StackPanel Spacing="5">
            <TextBlock Text="{x:Bind ViewModel.AccountName}"/>
            <TextBlock Text="{x:Bind ViewModel.AccountId}"/>
        </StackPanel>
        <Button Content="退出登录" Command="{x:Bind ViewModel.ExitLoginCommand}"/>
    </StackPanel>
</Page>
